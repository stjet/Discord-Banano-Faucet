{
  "name": "Discord-Banano-Faucet",
  "version": "1.0.0",
  "description": "A very simple discord banano faucet. Uses a discord bot. Needs two `.env` variables, `dbpass` and `token`. Dbpass is the mongodb database password, and token is the discord bot token. Please also change the mongodb connection string into `database.js`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@bananocoin/bananojs": "^2.4.19",
    "discord.js": "12.5.3",
    "mongodb": "^4.1.3"
  }
}
